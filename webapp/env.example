# App Mode: streamlit or fastapi
APP_MODE=streamlit

# Upload limits
UPLOAD_MAX_MB=200
MAX_FILES=500

# UI Language: en or gr
DEFAULT_LANGUAGE=gr

# Default options
DEFAULT_FILTER=false
DEFAULT_VERBOSE=false

# Server settings
STREAMLIT_PORT=8501
FASTAPI_PORT=8000
